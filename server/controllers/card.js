import Card from "../models/Post.js";
import User from "../models/User.js";

export const rngCard = async (req, res) => {
    try {
        //generate random number
        const rand = Math.floor(Math.random() * 1000000000000);
        //get card from
                                                                                             
        //create new card

        //send card
        
    } catch {
    
    }
}

export const rngCardBanner = async (req, res) => {
    
}

